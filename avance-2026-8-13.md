# Avance 2026-08-13 — Proyectos YadinStore en el Portafolio (mode porcelain)

**Fecha:** 2026-08-13
**Objetivo:** Integrar 3 proyectos de `C:\GITHUB\YadinStore` (e-Shop SaaS, Admin Dashboard y Back-end API) a la sección Proyectos del portafolio, trabajando SOLO sobre `CV_Fusion_FE_2026_PREVIEW.html` hasta aprobación del usuario (NO tocar `/src`).

---

## 1. Resumen Ejecutivo

Se agregaron **3 nuevas cards de YadinStore** al inicio de `.projects-grid` en el preview (`CV_Fusion_FE_2026_PREVIEW.html`), antes de la card VE&CO Web. Redacciones finales resumidas (co-redactadas con ChatGPT), títulos, badges, chips y links verificados. **`src/components/sections/Proyectos.jsx` NO fue tocado** — se replicará 1:1 solo tras aprobación del usuario.

Las 3 cards usan el mismo patrón de estilos del preview: `.pcard`, `.badge` (`.flag` para la ⭐), `.chips .chip`, `.link`.

---

## 2. Cards agregadas (estado final en preview)

### Card 1 — `⭐ e-Commerce SaaS · En desarrollo`
- **Título:** `YadinStore · e-Shop SaaS`
- **Link:** https://yadinstore-frontend.onrender.com/ (200 OK ✓)
- **Descripción:** con `<strong>` aplicados (YadinStore, Angular 21+Nx+MF, monolito Spring Modulith+hexagonal, Kafka 3.9.2, Outbox/DLT/idempotencia, Resilience4j, MongoDB ACID, Spring Security+JWT+RBAC+multi-tenancy+rate limiting+CORS+OWASP/IDOR, Docker, Testcontainers, CI/CD, observabilidad).
- **21 chips:** Java 21 · Spring Boot 3.5 · Spring Modulith · Angular 21 · Kafka 3.9.2 · MongoDB · Hexagonal Architecture · DDD · Transactional Outbox · DLT · Idempotency · Resilience4j · Spring Security · JWT · RBAC · Multi-Tenancy · OWASP · IDOR · Docker · Testcontainers · GitHub Actions

### Card 2 — `🔐 SaaS Admin · Multi-Tenant · En desarrollo`
- **Título:** `YadinStore · Admin Dashboard SaaS Multi-Vendor`
- **Link:** https://yadinstore-admin.onrender.com/ (verificado 200 ✓)
- **14 chips:** Angular 21 · TypeScript · Signals · Nx · Module Federation · Spring Boot 3.5 · Spring Security · JWT · RBAC · Multi-Tenancy · Rate Limiting · IDOR · MongoDB · Tailwind 4

### Card 3 — `⚡ Backend · Event-Driven · En desarrollo`
- **Título:** `YadinStore · Back-end API — Spring + Kafka`
- **Link:** https://yadinstore-backend.onrender.com/kafka-dashboard.html (200 OK ✓) — link dice "Ver Kafka Dashboard →"
- **20 chips:** Java 21 · Spring Boot 3.5 · Spring Modulith · Kafka 3.9.2 · MongoDB · Hexagonal Architecture · Transactional Outbox · DLT · Idempotency · Resilience4j · Spring Security · JWT · RBAC · Multi-Tenancy · OWASP · Rate Limiting · Mongock · Testcontainers · Docker · SASL_SSL
- Menciona que el broker se consulta **server-side con SASL_SSL y las credenciales NO se exponen al navegador**.

---

## 3. Análisis read-only de `C:\GITHUB\YadinStore` (evidencia verificada)

- **Monorepo:** `back-end/` (Spring Boot Maven), `front-end/` (Angular 21 + Nx + Module Federation, 4 MFE + Capacitor Android), `mfe-dashboard-admin/` (TailAdmin-based), `ci-cd-infra/` (docker-compose, Prometheus, Grafana, OTel), `yadinStore-Spec/` (specs + 12 ADR), `kafka_3.9.2/`, `MailHog/`.
- **Stack backend (pom.xml):** Java 21, Spring Boot 3.5.12, Spring Modulith 1.4.0, Kafka 3.9.2, jjwt 0.12.6, Mongock 5.5.1, MapStruct 1.6.3, springdoc 2.8.5, Resilience4j 2.3.0, OpenTelemetry 1.45.0, Testcontainers 1.20.6, JaCoCo 0.8.12 (umbral 80% línea/rama, Spotless 2.43.0).
- **Persistencia:** SOLO MongoDB (el README declara JPA pero no hay driver JPA en el pom; PostgreSQL 16 del compose es legacy).
- **Arquitectura verificada:** ADR-010 + paquetes `domain/application/infrastructure` + verificación con `ApplicationModules.verify()`.
- **Eventos:** Transactional Outbox (escribe en la MISMA transacción) + DLT; idempotencia: MailLog por eventId (índice UNIQUE), state machine de órdenes, webhook de pagos idempotente.
- **Seguridad:** Spring Security + JWT, BCrypt cost 12, rate limiting por IP, CORS restringido, multi-tenancy con IDOR, headers CSP/HSTS.
- **Admin:** datos reales de API, amCharts, JWT + RBAC estricto (admin/vendor), guards e interceptores (auth/error/tenant), `X-Tenant-ID`, ticket one-time 60s, remote MF, dark mode, Tailwind 4, Karma/Jasmine.

---

## 4. Decisiones de honestidad (con evidencia)

| Tema | Evidencia | Decisión |
|---|---|---|
| **Jenkins** | `plan-kafka-2026.md:36` → "Jenkins \| ❌ no hay Jenkinsfile"; `:272` → "[ ] Fase 3 — Jenkinsfile" (sin marcar); grep en `ci-cd-infra/` → **cero**; **no existe ningún Jenkinsfile** en el repo | **QUITADO** → sustituido por GitHub Actions (FE) + Render |
| **Azure** | `plan-kafka-2026.md:38` → "Azure \| ❌ cero"; `avance_07-08-2026.md:72` → Fase 3 "en pausa"; grep `ci-cd-infra/` → **cero** | **QUITADO** de la fase CI/CD del preview ("listo para deploy a Azure" retirado) |
| **Infrac real** | `ci-cd-infra/` (Prometheus, Grafana provisionado, OTel, Kafka, Mongo), GH Actions FE (`frontend-bootstrap.yml`), Render (FE+BE), Aiven Kafka (SASL_SSL) | En preview: FE → Docker + GitHub Actions + Render; BE → Aiven (SASL_SSL) + Docker |
| **Keycloak** | No está en el código del backend (solo roadmap ADR-008) | NO incluido en chips (el eShop OEM viejo es otro proyecto) |
| **URLs** | `yadinstore-backend.onrender.com` → 200; `yadistore-backend` (tipografía del usuario) → 404 | Se usó la URL correcta con la "n" |

---

## 5. Estado verificado (tras la aprobación y migración a JSX)

- **Preview:** `pcard: 13` (10 anteriores + 3 YadinStore) | `article open/close: 13/13` | `badge: 13` | `chip: 132` | `strong: 8/8` | `div: 260/260` | `section: 6` | `CV_Fusion_FE_2026_PREVIEW.html` (1297 líneas, raíz del repo).
- **`src/components/sections/Proyectos.jsx` (APLICADO 2026-08-13):** las 3 cards de YadinStore insertadas al INICIO del array `projects` (antes de VE&CO Web). Verificación estática: `title: 13`, `badge: 13`, `badgeClass: 13`, `chips: 13`, `href: 13`, `},`: 13 (13 objetos total).
- **Detalle técnico importante:** `<p className="text-gray-400 mb-4">{project.description}</p>` renderiza la descripción como TEXTO PLANO — el `<strong>` del preview NO se replica (se vería como texto literal). Las 3 descripciones van en texto plano, consistentes con las 10 cards existentes. Si se quieren negritas reales → usar `dangerouslySetInnerHTML` (cambio de componente, tradeoff a evaluar).
- **`badgeClass` asignados:** card ⭐ → `bg-green-500/20 text-green-300` (patrón flagship, como eShop OEM); cards Admin y Backend → `bg-green-500/10 text-green-500 border border-green-500/30` (patrón estándar).
- **Label de link:** en `Proyectos.jsx` el texto es FIJO ("Ver Proyecto →", línea 138) — el "Ver Kafka Dashboard →" solo existe en el HTML del preview; el href del Backend apunta a `kafka-dashboard.html`.
- **PDF del CV (`src/components/HV_YadinManrique_2026.pdf`):** actualizado por el usuario (2.53 MB, 2026-08-13 17:45) — git lo confirma como `modified`. El build lo empaqueta (2,532.87 kB) → el botón "Descargar HV .pdf (No ATS)" servirá la versión nueva al desplegar.

---

## 6. Cierre del día 2026-08-13 (build + commit + push)

- ✅ **PDF actualizado por el usuario** y verificado con evidencia (`git status` → `modified: src/components/HV_YadinManrique_2026.pdf`).
- ✅ **Build EXITOSO** (`npm run build` — vite 6.2.0): 51 módulos en 1.13s → `dist/` con:
  - `dist/assets/HV_YadinManrique_2026-BklImbEj.pdf` (2,532.87 kB — PDF nuevo del usuario)
  - `dist/assets/index-Dl9BVH0p.js` (241.73 kB, gzip 73.42) · `dist/assets/index-CNPy7DKe.css` (27.16 kB)
  - `dist/` está en `.gitignore` → NO se commitea.
- ✅ **Commit + push:** conventional corto → `feat: add YadinStore projects and updated CV PDF` (sin atribución AI). Incluyó `Proyectos.jsx`, el PDF y este avance + el preview HTML de la raíz (flag para el usuario: quedó trackeado bajo `git add .`).
- 🔲 (Solo si el usuario lo pide) `npm run deploy` (gh-pages -d dist) para publicar en producción.
- 🔲 Decisiones menores pendientes: chip `DDD` (card 1) y negritas reales con `dangerouslySetInnerHTML` (si se quieren).
