# Avance 2026-09-23 — Recovery 9:59: primer 200 tras serie 503 (wedge roto)

## 1. Recovery 9:59 Bogotá (datos USER, no re-ejecutado)

| Hora Bogotá 23-09 | Endpoint | Resultado | Latencia | Evidencia |
|---|---|---|---|---|
| 9:59 | BE `/actuator/health` | 200 OK | 2.55s | body `{"status":"UP","groups":["liveness","readiness"]}` |
| 9:59 | BE-JD `/api/jenkins/live` | 200 OK | 1.37s | body `serverTime 2026-09-23T14:59:00.961Z, outboxPending 0, kafkaPublishErrors 0` |

Serie previa: 7:50-7:51 429 `hibernate-rate-limited` → 8:00-9:31 503 continuo (pares BE/BE-JD) → 9:59 primer 200 en ambos.

## 2. Headers discriminantes

- 9:59 (app sana): `x-render-origin-server: Render`, `rndr-id: 75c5ff63`, CSP intacta.
- 7:50 (borde): `x-render-routing: hibernate-rate-limited` + `Server: cloudflare` + `Content-Type: text/plain` = emisor borde Render, nunca cruzó puertos primarios.

## 3. Veredicto: wedge roto

Clean/rebuild USER o wake natural (no determinable desde evidencia USER; ambos rompen el wedge `hibernate-wake-error`). Dominio exonerado: `outboxPending 0, kafkaPublishErrors 0`, regla de dependencia intacta, sin cambio de código requerido.

## DevSecOps (OWASP/ASVS)

- CSP/`X-Frame-Options: DENY`/`nosniff` intactos, CORS `allow-origin: https://ypmanrique2.github.io` (allowlist intacta), sin PII/secrets en body/headers.
- Estado de cambios: markdown únicamente, sin build, sin commit, sin push.
