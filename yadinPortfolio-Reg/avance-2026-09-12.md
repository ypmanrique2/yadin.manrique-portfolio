# Avance 2026-09-12 — Fix hitbox "Ver Proyecto →" + port PREVIEW→/src (sin stage, sin commit)

## 1. Causa-raíz del offset ~1rem (con evidencia, no inventada)

**NO existe un overlay fantasma persistente.** Verificación empírica con Chromium headless
(`elementFromPoint` sobre 13/13 enlaces, estado post-reveal):

- PREVIEW antes del fix: hitbox = franja exacta de **22px** sobre las palabras (`a.link`,
  chain limpio `a.link < article.pcard`, ningún pseudo/overlay encima). 16px arriba =
  `margin-bottom:16px` de `.chips` (muerto); 16px abajo = padding de la card (muerto).
- Los únicos `position:fixed/absolute` en pantalla con `pointer-events` peligrosos estaban en
  la sección timeline (`.timeline::before`, `.tl-item::before`, `.tl-item li::before`,
  líneas 599/619/665) — lejos de los links, pero endurecidos igual.
- `.pcard::after` (línea ~695) ya tenía `pointer-events:none`. `.reveal` resuelve a
  `translateY(0)` (identidad, sin desync). Test de lift `translateY(-5px) scale(1.005)`:
  desplazamiento neto medido **-1.65px**, el punto original sigue dando `a.link`.
- El mecanismo compatible con el reporte: blanco de ~22px + halo muerto de ~16px
  (en /src: `my-4` del link = 16px de margen muerto arriba/abajo, confirmado en el CSS
  compilado de Tailwind v4.1.18: `.my-4{margin-block:calc(var(--spacing)*4)}`; el margin
  NUNCA es hitbox) + lift de 4–5px en hover. La zona "viva" que el usuario encuentra
  ~1rem bajo las palabras es el borde inferior de la propia franja.

## 2. Fix aplicado (mínimo, reversible, estética 2026 intacta)

**`CV_Fusion_FE_2026_PREVIEW.html`**
- `.pcard .link` (líneas 812–830): `display:inline-block; position:relative; z-index:1;
  padding:8px 14px 8px 0; margin-top:2px` → el padding SÍ es hitbox. Medido post-fix:
  franja **22px → 38px**, viva continua de dy=-20 a +16 (antes solo -12..+8).
- `.pcard` (línea 696): `isolation:isolate` (stacking local, cero cambio visual).
- Pseudos decorativos (líneas 599/619/665): `pointer-events:none`.
- Typo propio del árbol (línea 1609): `<em></em>Transactional…</em>` → `<em>…</em>`
  (balance `em` 37/38 → 37/37).

**`src/components/sections/Proyectos.jsx`**
- Enlace (líneas 175–185): `my-4` (muerto) → `py-2 pr-4 pl-1 -ml-1` (vivo, ~40px de
  blanco); `relative z-10`; `rel="noopener"` (tabnabbing, era `null` en los 13);
  `aria-label="Ver proyecto: {title}"` (13 links homónimos para AT);
  `focus-visible` ring verde; `{project.linkLabel || "Ver Proyecto →"}`.
- Verificado: `my-4` = 0 ocurrencias restantes; `rel` en src = 5/5 anchors.

## 3. Port PREVIEW→/src (merge, sin pisar; diff sistemático 13 cards + skills + certs)

- Chips YadinStore (×2) y prosa: `K3d` → `k3d` (renombre del usuario en PREVIEW).
- eShop: `SpringBoot`/`SpringSecurity` → `Spring Boot`/`Spring Security` (nombres reales).
- Educativa badge → `Demo interactiva disponible` (intención más nueva del usuario en
  PREVIEW working tree; veredicto `git log -S`: df82eb2 viejo vs b9ffeb9 + edit actual).
- Backend `linkLabel`: `Ver monitor Kafka + Docker + K8s (k3d-AKS) + CI/CD & Observabilidad →`.
- `Certificaciones.jsx`: +2 certs que solo estaban en PREVIEW (líneas 26–47:
  `Introducción a Kubernetes` ID #49,528,106 y `Curso de Backend con Java y Azure`
  ID 573de605…, ago. 2026, con URLs de verificación) + `rel="noopener"` en template.
- `Inicio.jsx`: `rel="noopener"` en 3 anchors (GitHub/LinkedIn/Portafolio).
- **NO tocado (decisión documentada):** descripciones Backend/Insurance (JSX es chip-coherente:
  menciona Mongock/JaCoCo/Jenkinsfile/DinD/SASL_SSL/JasperReports/PostgreSQL que existen
  como chips; reemplazarlas huérfana chips) · `Habilidades.jsx` (superconjunto verificado
  de PREVIEW: 13+31+42 tags vs 11+19+11; niveles 1:1) · ATS ES/EN (edits del usuario) ·
  `Kafka 3.9.2` (no degradado a `Kafka` genérico).

## 4. Counts / verificación

- Links medidos: 13/13 PREVIEW + 13/13 /src. Hitbox PREVIEW: 22px → 38px.
- Tags PREVIEW: a 41/41, div 273/273, article 13/13, section 6/6, em 37/37 → PASS.
- JSON-LD PREVIEW: 3/3 bloques válidos. Glass/hover/clear-mode/print/aria intactos.
- Demos/PII toggle/hrefs: intactos (diff solo añade `linkLabel`; hrefs sin cambios).
- `rel="noopener"`: PREVIEW preexistente; src 0/5 → 5/5.

## 5. Porcelain final (TODO sin stagear, 0 staged, sin commit/push por prohibición)

```
M CV_Fusion_FE_2026_PREVIEW.html
M CV_Yadin_Manrique_ATS_2026.html        (del usuario, no tocado)
M CV_Yadin_Manrique_ATS_2026_EN.html     (del usuario, no tocado)
M src/components/sections/Certificaciones.jsx   (+23)
M src/components/sections/Inicio.jsx            (+3)
M src/components/sections/Proyectos.jsx         (+17/-)
M yadinPortfolio-Reg/avance-2026-09-10.md (del usuario)
M yadinPortfolio-Reg/avance-2026-09-11.md (del usuario)
?? yadinPortfolio-Reg/avance-2026-09-12.md (este archivo)
```

## 6. Pendientes de autorización (NO hacer sin OK de Yadin)

1. `git add` + conventional commit + push (prohibido en esta iteración).
2. Confirmar en navegador real (Chrome/Firefox) que el hover/clic ya cae sobre las palabras.
3. Decidir matiz descriptivo Admin (PREVIEW trae "+micro-frontend remoto/ACID") si se quiere fusionar.
4. Build (`npm run build`) — prohibido por regla del repo tras cambios.
