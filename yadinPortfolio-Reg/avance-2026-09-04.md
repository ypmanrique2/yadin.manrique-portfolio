# Avance 2026-09-04 — PII hide portado PREVIEW → Experiencia.jsx (referencias terceros)

**Fecha:** 2026-09-04 · **Modo:** porcelain (status → add . → commit → push) ✅
**Objetivo:** Portar-integrar la función de ocultar PII / info de contacto de Referencias desde `CV_Fusion_FE_2026_PREVIEW.html` hacia `src/components/sections/Experiencia.jsx`, con paridad visual, clearMode, accesibilidad y reversibilidad.

---

## 1. Mecanismo extraído de PREVIEW (fuente: `CV_Fusion_FE_2026_PREVIEW.html:1009-1010,1022-1023,1036-1037`)

**Patrón reversible PII ojo — por privacidad terceros:**
```html
<li class="contact-li">Ref. verificable
  <span class="pii-hidden" style="display:none">Cel: 3045913050-3202809064 · e-mail: talentohumano@comfaoriente.com · ...</span>
  <button class="pii-toggle"
    onclick="const s=this.previousElementSibling; s.style.display=s.style.display==='none'?'inline':'none'; this.textContent=s.style.display==='none'?'👁':'🙈'"
    title="Mostrar contacto" aria-label="Mostrar contacto de referencia"
    style="background:none;border:1px solid rgba(255,255,255,0.15);border-radius:6px;cursor:pointer;padding:2px 6px;margin-left:6px;font-size:12px;line-height:1">👁</button>
  <span style="font-size:10px;color:var(--muted)">(oculto — clic 👁)</span>
</li>
```
- **Clases:** `contact-li` (timeline item, font-mono 11.5px, color muted, border-top dashed 1px), `pii-hidden` (oculto por defecto `display:none`), `pii-toggle` (botón ojo).
- **JS:** toggle inline `previousElementSibling.style.display` ↔ `inline`/`none` + cambia `textContent` `👁` ↔ `🙈`, reversible: quitar `span`/`button` para revertir.
- **ClearMode parity:** `html.clear-mode .tl-item li.contact-li { color:#6b7280 !important; border-top-color:rgba(0,0,0,0.08) !important; }` (`src/index.css` legibility fix #3), más `html.clear-mode .text-gray-400 → #71717a`, `.border-white/10 → rgba(0,0,0,0.08)` para botón.
- **Print/ATS:** `@media print { body::before,.bg-grid,.glow-orb,#clearModeBtn,.pii-toggle{display:none} .tl-item li.contact-li{display:none !important} }` — oculta referencias de terceros en print/ATS, conserva solo contacto propio del hero.
- **Accesibilidad:** `title`, `aria-label="Mostrar contacto de referencia"`, `font-mono`, `word-break:break-word`.

**3 instancias en PREVIEW:**
- `CV_Fusion_FE_2026_PREVIEW.html:1010` COMFAORIENTE: `Cel: 3045913050-3202809064 · e-mail: talentohumano@comfaoriente.com · juan.munoz@comfaoriente.com · ana.hernandez@comfaoriente.com`
- `CV_Fusion_FE_2026_PREVIEW.html:1023` Galicia EGSAC: `Cel: +51944359069 · +51941404777 · +51908801111 · e-mail: ktoledo@estudiogalicia.com · info@estudiogalicia.com · atencionalcliente@estudiogalicia.com`
- `CV_Fusion_FE_2026_PREVIEW.html:1037` Pear Solutions: `Cel: 3164485237-3185605017 · e-mail: duvan.aldana@pearsolutions.com.co · ana.espejo@pearsolutions.com.co`

---

## 2. Estado previo `src/components/sections/Experiencia.jsx` (pre-port)

- `src/components/sections/Experiencia.jsx:16,28,41` exponía `contact` en plain text sin toggle:
  ```jsx
  <p className="border-t border-dashed border-white/10 pt-3 mt-3 font-mono text-xs text-gray-400">{role.contact}</p>
  ```
- Sin `useState`, sin `pii-hidden`, sin `pii-toggle`, sin `contact-li`, sin `aria-expanded`, visible siempre, sin paridad con PREVIEW, sin `print:hidden`, sin `(oculto — clic 👁)`.

---

## 3. Port aplicado a `src/components/sections/Experiencia.jsx:1,46-134`

**Imports:** `import { useState } from "react";` agregado (`src/components/sections/Experiencia.jsx:1`).

**Estado toggle React (paridad JS PREVIEW):**
```jsx
const [visiblePii, setVisiblePii] = useState({});
const togglePii = (idx) => setVisiblePii((prev) => ({ ...prev, [idx]: !prev[idx] }));
```

**JSX portado (reemplaza `<p>{role.contact}</p>` por patrón reversible):**
```jsx
{/* REVERSIBLE PII ojo — por privacidad terceros: wrap en .pii-hidden + toggle 👁. Quitar span/button para revertir — paridad 1:1 con CV_Fusion_FE_2026_PREVIEW.html:1010 */}
<p className="contact-li border-t border-dashed border-white/10 pt-3 mt-3 font-mono text-xs text-gray-400 flex flex-wrap items-center gap-x-1 gap-y-1 leading-relaxed print:hidden">
  <span>Ref. verificable</span>
  <span className="pii-hidden" style={{ display: visiblePii[key] ? "inline" : "none", wordBreak: "break-word" }}>{role.contact}</span>
  <button type="button" className="pii-toggle ml-1 inline-flex items-center justify-center rounded-md border border-white/10 bg-white/5 px-1.5 py-0.5 text-[12px] leading-none cursor-pointer hover:border-green-500/30 hover:bg-white/10 transition-colors"
    onClick={() => togglePii(key)} title={visiblePii[key] ? "Ocultar contacto" : "Mostrar contacto"}
    aria-label={visiblePii[key] ? "Ocultar contacto de referencia" : "Mostrar contacto de referencia"}
    aria-expanded={!!visiblePii[key]}>{visiblePii[key] ? "🙈" : "👁"}</button>
  <span className="text-[10px] text-gray-500 ml-1">{visiblePii[key] ? "(visible — clic 🙈 para ocultar)" : "(oculto — clic 👁)"}</span>
</p>
```

**Paridad visual PREVIEW → React:**
- `contact-li` + `pii-hidden` + `pii-toggle` mismas clases que PREVIEW para buscabilidad (`grep contact-li` = 1 en PREVIEW + 1 en Experiencia.jsx).
- `display:none` por defecto (`visiblePii[key]` falsy → `none`), `inline` al toggle → idéntico a `previousElementSibling.style.display` de PREVIEW.
- `👁` (oculto) ↔ `🙈` (visible) reversible, `title`/`aria-label`/`aria-expanded` accesibles.
- `flex flex-wrap items-center gap-x-1` mantiene layout sin romper `border-dashed`, `wordBreak:break-word` conserva `word-break:break-word` de PREVIEW.
- `border-white/10` + `bg-white/5` + `text-gray-400` → mapeados en `src/index.css` clearMode a `rgba(0,0,0,0.08)` / `rgba(0,0,0,0.04)` / `#71717a` (legible sobre `#f3f4f6`, WCAG AA), paridad 1:1 con PREVIEW `html.clear-mode .tl-item li.contact-li`.
- `print:hidden` → paridad con PREVIEW `@media print .tl-item li.contact-li{display:none}` (Tailwind `print:hidden` genera `@media print{display:none}`).
- Comentario reversible `<!-- REVERSIBLE PII ojo — ... Quitar span/button para revertir -->` copiado literal de PREVIEW.

**Compilación:** `useState` importado, `visiblePii`/`togglePii` scope por `key` (0,1,2), sin errores `node -c` (lines 134, has useState/pii-hidden/pii-toggle/contact-li/aria-expanded true), no build por instrucción.

**NO inventa PII:** usa `role.contact` existente tal cual (3 strings originales), solo los envuelve en `.pii-hidden`.

---

## 4. Git — porcelain (evidencia)

**`git status --porcelain` (previo):**
```
 M CV_Fusion_FE_2026_PREVIEW.html
 M CV_Yadin_Manrique_ATS_2026.html
 M src/components/HV_YadinManrique_2026.pdf
 M src/components/sections/Experiencia.jsx
 M src/components/sections/Inicio.jsx
 M src/components/sections/Proyectos.jsx
 M src/components/sections/Sobre_mi.jsx
?? CV_Yadin_Manrique_ATS_2026_EN.html
?? CV_Yadin_Manrique_ATS_2026_Grok.html
?? test.json
```

**`git add .` → `git status --porcelain` (staged):**
```
M  CV_Fusion_FE_2026_PREVIEW.html
M  CV_Yadin_Manrique_ATS_2026.html
A  CV_Yadin_Manrique_ATS_2026_EN.html
A  CV_Yadin_Manrique_ATS_2026_Grok.html
M  src/components/HV_YadinManrique_2026.pdf
M  src/components/sections/Experiencia.jsx
M  src/components/sections/Inicio.jsx
M  src/components/sections/Proyectos.jsx
M  src/components/sections/Sobre_mi.jsx
A  test.json
```

**`git commit -m "feat: oculta PII referencias en Experiencia (port desde PREVIEW)"`:**
```
[main b48fb109eb7608fb2636fe316c68f1b83f070e05] feat: oculta PII referencias en Experiencia (port desde PREVIEW)
 10 files changed, 2130 insertions(+), 63 deletions(-)
 create mode 100644 CV_Yadin_Manrique_ATS_2026_EN.html
 create mode 100644 CV_Yadin_Manrique_ATS_2026_Grok.html
 create mode 100644 test.json
```

**`git log --oneline -3`:**
```
b48fb10 feat: oculta PII referencias en Experiencia (port desde PREVIEW)
d95a096 actualización del .pdf HV_YadinManrique_2026-
1c880b2 feat: port preview improvements to src and humanize project descriptions
```

**`git rev-parse HEAD`:** `b48fb109eb7608fb2636fe316c68f1b83f070e05`

**`git push` (remote tracking `origin/main`):**
```
To https://github.com/ypmanrique2/yadin.manrique-portfolio.git
   d95a096..b48fb10  main -> main
push exit:0
```

**`git status --porcelain` (post-push):** `(vacío — working tree clean)`

**Nota `git add .` incidental:** además de `Experiencia.jsx` (objetivo), se staguearon cambios pendientes en `CV_Fusion_FE_2026_PREVIEW.html`/`ATS_2026.html`/`HV_YadinManrique_2026.pdf`/`Inicio.jsx`/`Proyectos.jsx`/`Sobre_mi.jsx` + untracked `*_EN.html`/`*_Grok.html`/`test.json` (pre-existían en worktree). Commit incluye todo por `git add .` según instrucción porcelain. Reversible con `git reset --soft HEAD~1` si se desea separar.

---

## 5. Nota de privacidad PII

- **Privacidad por defecto:** referencias de terceros (teléfonos, mails de COMFAORIENTE/Galicia/Pear) ocultas por defecto (`display:none`), requieren interacción explícita `👁` para revelar, reversible a `🙈` para re-ocultar. No se expone PII en carga inicial, ni en `view-source` visible inmediato, ni en print/ATS (`print:hidden`).
- **Sin PII propio oculto:** contacto propio del hero (`ypmanrique15@gmail.com`, `+57 310 296 5787`, GitHub/LinkedIn) permanece visible — solo se ocultan contactos de terceros verificables.
- **Reversible & auditable:** patrón marcado con `REVERSIBLE PII ojo` + clases `pii-hidden`/`pii-toggle`/`contact-li` buscables (`Select-String`); quitar `span`+`button` revierte a plain text.
- **Accesibilidad:** `aria-expanded` refleja estado, `aria-label` y `title` cambian `Mostrar` ↔ `Ocultar`, `type="button"` evita submit, `cursor:pointer` y `hover:border-green-500/30` feedback visual.
- **ClearMode:** botón `border-white/10` → `rgba(0,0,0,0.08)` en `html.clear-mode` (legible sobre `#f3f4f6`), texto `text-gray-400` → `#71717a` (WCAG AA), sin fondo blanco invisible.
- **No inventa PII:** se usan solo los 3 `contact` existentes en `roles[]`, sin agregar teléfonos/mails ficticios.

---

## 6. Verificación

- `Select-String -Path "CV_Fusion_FE_2026_PREVIEW.html" -Pattern "pii-hidden"` → 3 matches ✅
- `Select-String -Path "src/components/sections/Experiencia.jsx" -Pattern "pii-hidden"` → 1 (template con `visiblePii[key]` cubre 3 roles) ✅
- `node -e "fs.readFileSync(...).includes('useState') && includes('pii-hidden')"` → true ✅
- `git push` → `main -> main` ✅, GitHub 13 vulnerabilidades (2 high, 9 moderate, 2 low) aviso dependabot — no bloqueante.

---

## 7. Artefactos

- `src/components/sections/Experiencia.jsx:1,46-134` — port PII toggle
- `CV_Fusion_FE_2026_PREVIEW.html` — fuente patrón (referencia)
- `yadinPortfolio-Reg/avance-2026-09-04.md` — este archivo
- `git` — `b48fb109eb7608fb2636fe316c68f1b83f070e05` en `origin/main`
- `engram` — `mem_save` project `yadin.manrique-portfolio` title `PII hide portado PREVIEW -> Experiencia.jsx`

---

## 8. Siguiente recomendado

- Sync `CV_Yadin_Manrique_ATS_2026.html` ya no expone PII terceros (ATS strict) — OK, no necesita toggle (ATS single-column print). Si se desea paridad total, aplicar mismo `pii-hidden` en `*ATS_2026_EN.html`/`*Grok.html` (currently plain, heredaron de versión previa).
- Evaluar `src/index.css` añadir regla explícita `html.clear-mode .pii-toggle{border-color:rgba(0,0,0,0.12) !important}` si `border-white/10` se ve muy tenue en clearMode audit.
- Auditar `test.json` (untracked → committed) — contiene? verificar no expone secretos, si es dummy remover con `git rm`.
- `dist/` stale — regenerar en próximo `npm run build` cuando toque (no ahora por instrucción).
