# Avance 2026-09-10 — Extensión de estilo a proyectos restantes (PREVIEW)

> Archivo: `CV_Fusion_FE_2026_PREVIEW.html` (único archivo tocado).
> PROHIBIDO en esta iteración: `git add` / `commit` / `push`. No se hizo build.

## 1. Estilo nuevo detectado (cards del usuario, NO pisadas)

4 cards guía de estilo, verificadas intactas post-edición (grep de firmas únicas OK):

| Card | Firma verificada | Patrón de estilo |
|------|------------------|------------------|
| YadinStore e-Shop SaaS (l.1586) | `Micro-Frontends` 1x, `Transactional Outbox Pattern` 1x | Verbo activo + término ES (English Term), ~150 palabras, `<em>` producto, 30 chips |
| YadinStore Admin (l.1635) | `One-Time Ticket Access` 1x | Idem anterior, consola multi-tenant, `demo-creds` solo lectura, 30 chips |
| YadinStore Backend (l.1678) | `Jenkins 2.468.2` 1x, `JasperReports` 3x | ES técnico denso sin paréntesis bilingües, capas BE→seguridad→datos→CI/CD→obs., 33 chips |
| Insurance CRM (l.1720) | `reingeniería de flujos` 1x, `Secure Coding` 1x | 3 pilares (frontend desacoplado → API → CI/CD/deploy) + cierre evidencia (`Este desarrollo evidencia…`), 27 chips |

Reglas extraídas y aplicadas al resto: (a) abrir con `Diseñé e implementé / Contribuí`;
(b) concepto ES + `(English Term)`; (c) `<em>` para nombre de producto/patrón cabecera;
(d) seniority IMPLÍCITA (stack + responsabilidad + estándares), nunca decir "senior";
(e) chips append al final, formato `.chip` y orden existente conservados.

## 2. Proyectos tocados (descripción reescrita + chips)

| Proyecto (línea `h3` final) | Chips antes → ahora | Chips agregados |
|-----------------------------|---------------------|-----------------|
| VE&CO Web (l.1756) | 8 → 16 | RxJS, REST API, Firebase Auth, JWT, RBAC, Responsive Design, CI/CD, SEO |
| VE&CO App PlayStore (l.1781) | 10 → 17 | RxJS, REST API, Firebase Auth, JWT, RBAC, Responsive Design, CI/CD |
| eShop OEM (l.1806) | 9 → 15 | SSR, RBAC, OWASP, Clean Architecture, REST API, Docker (+typo `NextJs`→`Next.js`) |
| Portafolio adaptable (l.1831) | 5 → 10 | TypeScript, RxJS, REST API, Responsive Design, SEO |
| Landing Brevo (l.1851) | 10 → 15 | HTML5, Responsive Design, Security Nonces, Hooks, Custom Post Types |
| Landing AI First (l.1874) | 8 → 16 | ACF Pro, PHP, Hooks, dbDelta, Responsive Design, Security Hardening, Clean Architecture, Custom Post Types (+typo `Wordpress`→`WordPress`) |
| Pokédex / App educativa (l.1898) | 8 → 13 | MySQL, CORS, Route Guards, REST API, Responsive Design |
| Intranet (l.1920) | 5 → 10 | RBAC, IAM, OWASP, Responsive Design, Google Workspace |
| Página negocios (l.1942) | 3 → 8 | Responsive Design, SEO, GitHub Pages, Semantic HTML, Accessibility |

Notas de honestidad técnica (no inflar): Página negocios se declara `Lightweight / complejidad básica`;
Portafolio/Intranet mantienen su stack real (sin inventar Kafka/Java donde no existe);
VE&CO App NO suma Capacitor (empaquetado no confirmado) — solo skills del stack declarado.

## 3. Bugfix

- Badge Insurance CRM l.~1749: `�️ Business Process Digitalization` → `🛡️ …`
  (carácter replacement U+FFFD suelto; solo badge, descripción y chips intactos).
- Typos en chips: `NextJs`→`Next.js`, `Wordpress`→`WordPress`.

## 4. Verificación (Select-String / regex, sin build)

- `chip spans`: **171 → 203 (+32)**. `tag spans`: 37 (sin cambio).
- Balance tags: `article` 13/13, `div` 273/273, `section` 6/6, `p` 13/13,
  `em` 13/13 (11 pares nuevos, todos cerrados), `strong` 2/2.
- Caracteres `�` en archivo: **0**.
- JSON-LD: 3/3 bloques parsean válido (Person + ItemList + FAQPage, sin tocar).
- `href` malformados/vacíos en proyectos: 0. Ningún link creado ni modificado.
- PII: sin cambios (refs terceros siguen `display:none` + toggle 👁; print las oculta).
- ATS: sin tablas agregadas, grid print single-column intacto, `clear-mode`, `aria`, SEO head/canonical/og sin tocar.

## 5. git status --porcelain final

```text
M CV_Fusion_FE_2026_PREVIEW.html        (modificado SIN stagear — como se pidió)
D yadinPortfolio-Reg/avance-10-09-2026.md
D yadinPortfolio-Reg/avance-2026-01-10.md
?? "yadinPortfolio-Reg/avance-2026-09-10 parte 1.md"
?? "yadinPortfolio-Reg/avance-2026-09-10 parte 2.md"
?? yadinPortfolio-Reg/avance-2026-09-10.md   (este archivo)
```

Nota: el `M` del PREVIEW incluye cambios previos del usuario (ya estaba `M` antes de
esta sesión) + los de hoy. Los `D` y `?? parte 1/2` son preexistentes, no tocados por
este sub-agente. `git diff --stat` vs HEAD es grande por historial previo, no por esta
iteración (9 hunks de cards + 1 badge).

## 6. Optimizaciones pendientes de AUTORIZACIÓN (para pasar a /src)

1. Portar las 9 descripciones + 32 chips al FE (`src/`) 1:1 cuando autorices.
2. Sincronizar `knowsAbout` JSON-LD con chips nuevos (ej. `Security Nonces`, `dbDelta`,
   `Route Guards`, `Semantic HTML`) — hoy NO se tocó el head a propósito.
3. Ampliar `ItemList` JSON-LD con los proyectos que hoy faltan (solo lista 6 URLs).
4. Revisar `experience`: `h3` "Back" (l.1527, título truncado) y "TECNÓLOGO…" (l.1503) —
   fuera de scope de esta tarea, requieren tu redacción.
5. Unificar criterio `SQL` vs `MySQL` en Pokédex (hoy coexisten ambos chips; propuesta:
   dejar solo `MySQL`).
6. Decidir `parte 1 / parte 2` de avances (¿consolidar en este archivo y borrar?) y los
   dos `avance-*.md` en `D` (¿restaurar con `git checkout --` o asumir borrado?).
7. Cuando autorices: commit convencional con solo `CV_Fusion_FE_2026_PREVIEW.html` +
   este avance (nada stageado por ahora).

---

# Sesión sub-agente 2026-09-11 (noche) — Integra toques usuario + Habilidades abajo + sync ATS ES/EN

Commit: `feat: integra toques usuario + unifica Habilidades + sync ATS ES/EN` (push origin main).
Reglas: sin Co-Authored-By, conventional commits, sin build, sin tocar .skill files.

## 1. Toques del usuario revisados (no a ciegas) e integrados
- `Proyectos.jsx` + `CV_Fusion_FE_2026_PREVIEW.html`: título
  `YadinStore · e-Shop SaaS · Enterprise e-Commerce Engine` → `…Enterprise e-Commerce`
  (solo título; descripción conserva "SaaS e-Commerce Engine" como término de arquitectura
  en AMBOS archivos — consistente, se respeta).
- `D yadinPortfolio-Reg/CV_Yadin_Manrique_ATS_2026_Grok.html`: borrado del usuario.
  Decisión: NO restaurar. Evidencia: (a) 0 referencias a "Grok" en *.html/*.jsx/*.js del repo
  (grep exhaustivo); (b) era variante condensada de 4 cards, NO fuente del PDF
  (el PDF sale de `CV_Yadin_Manrique_ATS_2026*.html` de raíz, intactos);
  (c) nada lo enlaza → ningún link roto. Restore si cambiás de idea:
  `git checkout 13df7f0 -- yadinPortfolio-Reg/CV_Yadin_Manrique_ATS_2026_Grok.html`.
- `D avance-10-09-2026.md` + `D avance-2026-01-10.md`: NO es pérdida.
  Verificado byte-idénticos (diff 0) a los untracked
  `avance-2026-09-10 parte 1.md` / `parte 2.md` → fue un RENAME del usuario.
  Se commitea como rename (D + add).
- Recomendación pendiente (tuya, orquestador/usuario): consolidar `parte 1/2` en un solo
  avance y borrar duplicados — NO lo hice unilateralmente.

## 2. Hallazgo: divergencia PREVIEW ↔ JSX (resuelto sin pérdida)
- El port 13df7f0 NO fue 1:1 en chips (JSX quedó superset en varias cards; PREVIEW curó
  pins: `Kafka 3.9.2`→`Kafka`, `k3d`→`K3d`, etc.). Doctrina aplicada: UNION con PREVIEW
  como base de orden + extras JSX preservados (merge sin pérdida, nada borrado).
- Tras esta sesión: **PREVIEW ≡ Proyectos.jsx (118 chips únicos c/u, diff 0 en ambos
  sentidos)**. Único cambio real en JSX: typos `NextJs`→`Next.js`, `Wordpress`→`WordPress`
  (PREVIEW ya los tenía bien desde la sesión de estilo).
- PREVIEW e-Shop/Admin usan `K3d`; Backend usa `k3d` (inconsistencia heredada de la fuente,
  se espeja tal cual, no se "corrige" la fuente sin tu autorización).

## 3. Habilidades abajo (tagGroups) — port de nuevas chips
- Renombres ATS-literales: `Clean Arch`→`Clean Architecture`, `Hexagonal`→`Hexagonal Architecture`
  (igual en ATS ES/EN; mejora match literal de parsers sin cambio estructural).
- Cloud +7: Spring Modulith, Spring Security, Transactional Outbox, Resilience4j,
  Testcontainers, Virtual Threads, X-Tenant-ID.
- Frontend +10: JavaScript, HTML5, CSS3, AJAX, jQuery, Hooks, Security Nonces, dbDelta,
  Google Workspace, Jasmine / Karma. (`Hooks` cubre WP-hooks de landings y React-hooks
  de eShop/CRM — doble lectura honesta, ambas evidenciadas en código.)
- Skips deliberados (YAGNI / ruido / nicho): AI first, SEO human in the loop, DLT
  (ambiguo: dead-letter vs ledger), Gunicorn, Mongock, SASL_SSL, Jenkinsfile,
  Pipeline as Code, DinD, PlayStore, Capacitor/Ionic (no confirmado — ver §5),
  pins versionados (cubiertos sin versión).
- Total abajo: 58 → 87 tags (13 + 31 + 43).

## 4. Unificar Habilidades en una sola sección — evaluación con tradeoffs
- Overlap real arriba↔abajo ≈ 2 filas (Azure, Docker/K8s) e intencional (nivel vs keyword).
  El problema de consistencia es TRIANGULAR (PREVIEW↔JSX↔Habilidades↔ATS×2↔knowsAbout),
  no binario arriba/abajo.
- Opción A (elegida): mantener dos bloques. Pro: conserva señal de seniority
  (dots, escaneo humano ~6s) + cobertura ATS (tags); churn mínimo; print/aria intactos.
  Contra: requiere regla de sync (abajo = proyección curada de la unión de chips).
- Opción B (rechazada): unificar en una sola nube/tags. Pro: una sola fuente visual.
  Contra: pierde señal de nivel; rediseño + retest; NO resuelve la divergencia triangular;
  viola YAGNI para el objetivo actual (PDF vía ATS). Costo > beneficio.
- Regla adoptada: Habilidades-abajo = proyección curada (no espejo 1:1) de la unión
  de chips de proyecto; formas completas para match ATS literal.

## 5. Sync ATS ES + EN (raíz, fuentes del PDF)
- Skills groups espejados con Habilidades.jsx (renombres + Cloud +7 + Frontend +22,
  incluyendo los 12 tags CRM-era que ATS nunca había recibido).
- Cards (aditivo + dedup corto→largo): ES/EN e-Shop +18 (dedup Hexagonal/Outbox),
  Admin +25, Backend dedup Hexagonal (ya en paridad), eShop-OEM +3
  (Java, SpringBoot, SpringSecurity). Resto de cards ya en paridad (verificación previa
  fallaba por bug de regex, ver §6). Extras propios ATS preservados
  (Capacitor, Ionic, CORS, IAM, Jenkinsfile, Module Federation, Nx…).
- `k3d` minúscula duplicada eliminada en e-Shop/Admin (canónico `K3d` de la fuente).
- `knowsAbout` +10 (nivel-persona, senior-weighted): Spring Security, Testcontainers,
  Virtual Threads, X-Tenant-ID, Clean Architecture, JavaScript, HTML5, CSS3,
  Multi-Tenancy, Idempotency. PREVIEW head NO tocado (decisión previa vigente).
- ⚠️ FLAG honestidad (requiere tu confirmación, NO borrado unilateral): ATS card
  VE&CO-App lista `Capacitor, Ionic` pero PREVIEW/JSX los excluyen a propósito
  (empaquetado no confirmado, avance-2026-09-10 §2). Si no se confirma con código,
  quitar de ATS ES/EN.
- Títulos ATS YadinStore NO unificados con PREVIEW (variantes válidas preexistentes;
  el cambio "Engine" del usuario no aplica ahí porque no existe esa palabra).
  Descripciones/links/PII/toggle 👁/clearMode/aria/print:hidden intactos (diff).

## 6. Bug encontrado y fixeado (regex spans multilínea)
- Los scripts de conteo/sync usaban `<span class="chip">` literal y eran ciegos a
  `<span\n class="chip">` (tags que envuelven línea, ~10% de los chips). Primera corrida
  del sync ATS duplicó ~47 chips (detectado en revisión del diff, NO commiteado).
- Fix: `git checkout --` de ambos ATS, regex `<span\s+class="chip…">`, re-run +
  auditoría de dups case-insensitive + colisión corto/largo. Resultado: 0 dups.
- Lección: todo conteo/diff de chips debe usar `\s+` tras `<span`; verificar siempre
  con `git diff` antes de commit (así se atrapó).

## 7. Verificación final (sin build, por regla del repo)
- Balance tags OK: ATS ES/EN article 16/16, div 103/103, span 475/475, p 13/13,
  section 6/6, em 7/7; PREVIEW article 13/13, div 273/273, span 411/411.
- Chips: ATS ES/EN 250 spans c/u; PREVIEW 239 (incl. `chip accent`); JSX 118 únicos.
- JSON-LD válido (parse): ATS ES/EN Person 1/1; PREVIEW Person+ItemList+FAQPage 3/3.
- JSX: 13 titles, 13 href, 2 demo, braces/parens/brackets balanceados, sin `<em>`,
  imports/exports intactos, 0 typos. U+FFFD: 0 en los 3 HTML.
- Habilidades.jsx: braces/parens OK, export OK, 87 tags abajo.

## 8. Archivos, líneas y hashes (post-cambio)
- `src/components/sections/Proyectos.jsx` — 190 lín (typos ×2 + título usuario).
- `src/components/sections/Habilidades.jsx` — 122 lín (renombres ×2, +29 tags).
- `CV_Yadin_Manrique_ATS_2026.html` — 1121 lín (+48 diff).
- `CV_Yadin_Manrique_ATS_2026_EN.html` — 1092 lín (+48 diff).
- `CV_Fusion_FE_2026_PREVIEW.html` — 2474 lín (solo toque usuario, 1 línea).
- (ver hashes + porcelain final en sesión; script de sync en /tmp, no commiteado:
  `ats_sync.py` — re-ejecutable, documentado aquí).

## 9. Pendiente / next (para orquestador-usuario)
1. Confirmar Capacitor/Ionic en VE&CO-App (código) → quitar o evidenciar en ATS.
2. `SQL` vs `MySQL` en Pokédex (coexisten; propuesta: solo MySQL).
3. ItemList JSON-LD del PREVIEW (6 URLs vs 13 proyectos) + `experience` h3 "Back"
   truncado — fuera de scope, requieren tu redacción.
4. Consolidar avances `parte 1/2` vs borrados (rename ya commiteado).
5. Revisión visual rápida del portfolio (dev server, solo lectura).

---

# § Plan ATS — CV_Yadin_Manrique_ATS_2026 ES/EN (explore-only 2026-09-11, SIN editar CVs)

> Sub-agente sdd-explore. Leídos ES (1149 lín) + EN (1120 lín) por ventanas.
> Skill k8s-basics cargado. Sin commit/push. Plan guardado en engram
> `sdd/cv-ats-2026-plan` (project `yadin.manrique-portfolio`).

## A. Gaps CV vs LinkedIn (cruce)

1. **Headline**: role-line CV sin Docker/K8s, SDD/MCP/LLM, OWASP, SQL/MongoDB
   (LinkedIn 220c sí los trae). Alinear sin stuffing.
2. **Años**: ES hero `5+` / EN meta `6+ yrs` / LinkedIn `6 años` → unificar a
   `5+` honesto conservador (2019–2025 con gaps). Fix EN meta.
3. **Cargos**: h3 `Back` truncado (ES l.678 / EN l.676); `TECNÓLOGO…` es título
   de grado, no rol. Renombrar a rol real en fase2.
4. **EN con español**: EN l.908–919 `Intermedio-Avanzado`/`Avanzado`/`CI-CD` →
   normalizar a English.
5. **Stuffing**: eShop-OEM dups `Spring Boot`+`SpringBoot`,
   `Spring Security`+`SpringSecurity`; YadinStore 3 cards repiten ~30 chips →
   flagship full + 2 slim.
6. **Certs 21 → 12-14**: falta ID `821af49a` (Reactiva lumped bajo `876f9324`),
   SEO IA `nv69c7cy`, HPE `H40ZMS`, LLMs `383901b5`, Data Science `s6d1re4r`;
   Hacking Ético sin ID texto (`oawvtcmq` solo en URL). Resto 146 → LinkedIn/portfolio.
7. **JSON-LD**: solo Person; `knowsAbout` 41 sin SDD/MCP/LLM/ITIL/ASVS/IDOR;
   `hasOccupation.skills` 11 truncado. Falta EducationalOccupationalCredential,
   Employment, ItemList/CreativeWork, llms.txt, robots, sitemap, og-image.
8. **PII terceros**: ATS limpio OK (toggle 👁 solo portfolio). Cooguasimales 2019
   + INDETEXCA 2018-2019 omitidos a propósito — documentar, NO re-agregar.
9. **FLAG previo vigente**: `Capacitor, Ionic` en VE&CO-App ATS sin evidencia en
   PREVIEW/JSX → confirmar con código o quitar (avance §5.166).

## B. Plan por fases (para sdd-propose)

- **F1 headline+summary**: role-line + keywords recruiter (Java21 SpringBoot
  Kafka KRaft Angular React MongoDB Docker K8s AKS k3d Azure GCP RBAC JWT OWASP
  MCP SDD ITIL), densidad sin stuffing, fix `5+`/`6+`.
- **F2 experiencia STAR honesto**: STAR + MM/YYYY + cargos reales + métricas
  (15→2–3min Galicia, SLA95% BMC Helix, ISO9001/NTC 6744, GCP hardened).
- **F3 proyectos**: flagship e-Shop full, Admin/BE slim dedup; dedup
  corto/largo (`SpringBoot`→`Spring Boot`); resto cards intactas.
- **F4 skills unificadas**: arriba nivel + abajo keywords formas completas;
  regla sync PREVIEW↔JSX↔ATS×2↔knowsAbout; fix EN español.
- **F5 certs**: top 12–14 verificables con IDs/links (ITIL GR671609055YM, K8s
  49528106, Java-Azure 573de605, Storage 49476429, MCP-Adv 49422238, Docker
  9647301a, DevOps 31317567, Java Prof 876f9324 + Reactiva 821af49a, deploy
  Azure 41d75233, servidores b2ee76e1, Agentes IA 9e058c43, MCP empresas
  9a7bc1be); resto → LinkedIn/portfolio.
- **F6 JSON-LD SEO AI First**: Employment + Credentials + CreativeWork/ItemList,
  `knowsAbout` += SDD/MCP/LLM/ITIL/ASVS/IDOR, `hasOccupation.skills` ampliado,
  llms.txt + robots + sitemap + og-image. PREVIEW head se toca solo aquí.
