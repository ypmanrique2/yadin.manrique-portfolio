# Avance 2026-09-11 — Port PREVIEW → Proyectos, Habilidades y ATS (ES/EN/Grok)

## Qué se portó
Port 1:1 del lienzo fuente `CV_Fusion_FE_2026_PREVIEW.html` (13 pcards, estilo bilingüe
ES (English Term) con verbo activo, seniority implícita) a los 4 destinos:

1. **`src/components/sections/Proyectos.jsx`** (13/13 proyectos):
   - YadinStore e-Shop + Admin: descripciones nuevas estilo bilingüe (MFE, Hardened Security
     Architecture, One-Time Ticket, Anti-IDOR, Multi-Tenancy Isolation, K3d→AKS), chips 17–22 → **30** c/u
     (X-Tenant-ID, NgRX, Kubernetes (K8s), Angular Signals, Virtual Threads, EDA, AKS…).
   - YadinStore Backend: solo badge → texto PREVIEW (descripción/chips ya estaban 1:1).
   - Insurance CRM: sin cambios de contenido (ya 1:1); **bugfix typo** `Datan Sanitization` → `Data Sanitization`.
   - Las otras 9 (VE&CO web/app, eShop, portafolio, 2 landings, app educativa, intranet, página):
     descripciones extendidas + chips 3–10 → **8–17** c/u, badges PREVIEW.
   - `href` y credenciales demo **intactos** (nada inventado). Estructura/imports/exports/className intactos.
2. **`src/components/sections/Habilidades.jsx`**: tags cortos ATS-friendly nuevos —
   Especialidades +`EDA, Idempotency`; Cloud +`JWT, IAM, Multi-Tenancy, IDOR, Rate Limiting`;
   Frontend +`NgRX, SSR, Firebase Auth, Route Guards, Responsive Design, Semantic HTML,
   Accessibility, Security Hardening, Custom Post Types, GitHub Pages`.
3. **`CV_Yadin_Manrique_ATS_2026.html`** (ES): eShop + VE&CO web/app con contenido extendido y
   chips expandidas (app conserva `Capacitor, Ionic` que PREVIEW no lista — merge sin pérdida);
   **6 cards nuevas** (portafolio, landing Brevo, landing AI-first, app educativa, intranet, página)
   → 7 a **13 cards**; mismos tags nuevos de skills. Single-column, print CSS y JSON-LD intactos.
4. **`CV_Yadin_Manrique_ATS_2026_EN.html`**: mismo port traducido (13 cards, tags).
5. **`yadinPortfolio-Reg/CV_Yadin_Manrique_ATS_2026_Grok.html`**: variante condensada preservada
   (4 cards, sin split); eShop/VE&CO/Portafolio-CRM enriquecidos + mismos tags. Decisión: no se
   expande a 13 para no romper su naturaleza de variante sintética.
6. **Bugfix PREVIEW L1818**: `<span class="chip">REST API</span><span </div>` → `</div>`
   (span sin cerrar). Único cambio al lienzo fuente además de nada más.

## Archivos + líneas
- `src/components/sections/Proyectos.jsx` — 190→188 líneas (SHA256 56F8CFFA55C6)
- `src/components/sections/Habilidades.jsx` — 122 líneas (SHA256 A06D7B34B746)
- `CV_Fusion_FE_2026_PREVIEW.html` — 2474 líneas, bugfix 1 línea (SHA256 B3C268ED6A8C)
- `CV_Yadin_Manrique_ATS_2026.html` — 941→1016 líneas (SHA256 3950AF382086)
- `CV_Yadin_Manrique_ATS_2026_EN.html` — 906→987 líneas (SHA256 1E8754391CAF)
- `yadinPortfolio-Reg/CV_Yadin_Manrique_ATS_2026_Grok.html` — 846→854 líneas (SHA256 B0AFA47D7E9D)

## Chips / counts
- PREVIEW: 252 chips / 13 pcards (tras bugfix span 411/411 balanceado).
- ATS ES/EN: 220 chips / 13 cards proyecto (+3 jobs = 16 articles). Grok: 0 chips (condensada, sin chips por diseño).
- Proyectos.jsx: 13 `title:`, braces 32/32, parens 73/73, brackets 16/16, sin `<em>`/typos.
- Habilidades.jsx: braces 54/54, parens 31/31, export + 3 grupos intactos.

## Verificación funcional
- Balance tags OK: article/div/span/p 13/13 (PREVIEW), 16/16 + p 13/13 (ATS ES/EN), 9/9 (Grok).
- JSON-LD válido (ConvertFrom-Json): PREVIEW Person+ItemList+FAQPage; ES/EN/Grok Person. `knowsAbout` e
  `itemListElement` son arrays; `hasOccupation.skills` string se deja intacto (válido).
- `src`: solo cambiaron textos `badge:`/`description:`/`chips:`; imports, exports, `href:`, `demo:`,
  `badgeClass:`, className, keys y mapeos intactos → compilable a nivel sintaxis (sin `npm run build`
  por regla del repo). PII toggle 👁, clearMode parity, aria, `@media print/responsive` no tocados
  (diff confirma cero cambios en esas zonas salvo reformateo preexistente del usuario en PREVIEW).
- Reglas repo respetadas: sin `Co-Authored-By`, conventional commits, sin build, sin tocar `.skill` files.

## Porcelain final
(ver `git status --porcelain` post-commit en sesión; archivos del port agregados al commit feat)

## Pendiente / next
- Revisar visual rápido del portafolio (dev server local) — solo lectura, sin build.
- Orquestador: decidir si ItemList JSON-LD del PREVIEW se amplía a 13 proyectos.

---

# Sesión SDD-apply cv-ats-2026 — F1→F6 en CV_* (sin commit/push/verify)

## Alcance respetado
Solo `CV_Yadin_Manrique_ATS_2026.html`, `CV_Yadin_Manrique_ATS_2026_EN.html`,
`CV_Fusion_FE_2026_PREVIEW.html` + este avance. Sin `/src`, sin `git add/commit/push`,
sin build, sin `Co-Authored-By`. Skill k8s-basics cargada: headline distingue runtime
(Docker/Testcontainers) de orquestación (k3d local · AKS prod) + señal cloud-native/stateless.

## F1 Headline (role + title/meta/OG/keywords en los 3)
- `.role` ES/EN: `… TypeScript · Docker · K8s (k3d · AKS) · SDD/MCP · OWASP · SQL/MongoDB · Cloud SaaS`.
  PREVIEW glass: misma señal (formato `/` propio del lienzo).
- `title`, `meta description`, `og:title/description` alineados; `keywords` +`SQL, k3d, AKS, SDD, MCP`;
  `og:locale` nuevo (`es_CO` / `en_US`). EN keywords `Cúcuta`→`Cucuta`.

## F2 Consistencia 5+ honesto
- EN `6+ yrs`→`5+ yrs` (meta description + og:description). `grep 6+` = 0 en los 3.
- Extra honesto: `<h3>Back</h3>` trunco → `Backend / Soporte de Datos` (ES/PREVIEW),
  `Backend / Data Support` (EN). Desviación documentada, reversible.

## F3 i18n EN total
- `addressLocality` + contacto `Cúcuta`→`Cucuta`; `Intermedio-Avanzado`→`Intermediate-Advanced`;
  2× `Avanzado`→`Advanced`; títulos `Introducción a Kubernetes`→`Introduction to Kubernetes`,
  `Curso de Backend con Java y Azure`→`Backend Development with Java and Azure` (marca
  `Código Facilito` intacta por ser nombre propio); comentarios `<!-- ABOUT/EXPERIENCE/
  PROJECTS/SKILLS/CERTIFICATIONS/EDUCATION -->`. `grep Cúcuta|años|Tecnólogo|Intermedio|Avanzado` = 0.

## F4 De-stuffing
- `SpringBoot`→`Spring Boot`, `SpringSecurity`→`Spring Security` (3 archivos) = 0.
- Dedup card YadinStore e-Shop (ES/EN): fuera `Spring Boot` y `Kafka` duplicados (ya estaban
  `Spring Boot 3.5`/`Kafka 3.9.2` como accent). `K3d`→`k3d` unificado; PREVIEW `Mongo DB`→`MongoDB`.

## F5 Certs 21→13+8 (solo verificables, con ID/link o declaración sin-enlace)
- Visibles 13 (K8s/AKS, Backend Java+Azure, Azure Storage, MCP Advanced, ITIL, Docker,
  DevOps, Java Reactiva, Agentes IA, MCP empresas, Ciberseguridad, Spring, Signals/RSC).
  Ciberseg/Spring/Signals/Git se promovieron a visibles (antes 15-18); Git quedó en details
  por cupo 13 (decisión reversible del apply).
- `<details class="ats-more"><summary>+8…</summary>` con Despliegue-Azure, Deploy-servidores,
  IA-0-a-Prod (sin enlace público declarado), Apps-IA-C#, Git, MEAN, OCI, ONE.
  CSS `.ats-more` + script `beforeprint/afterprint` (auto-expande al imprimir; parseable
  por ATS en fuente en todo caso). h2: `13 destacadas + 8 adicionales` / `13 featured + 8 more`.

## F6 JSON-LD + SEO head + seguridad links
- ATS ES/EN: `Person` extendida (`alumniOf:SENA`, `hasCredential:ITIL 4/PeopleCert`,
  `worksFor:COMFAORIENTE`) + `ItemList` 6 flagship + `FAQPage` 3 Q (stack/seniority-5+/
  verificación; SIN credenciales — las de demo se comparten en entrevista).
- PREVIEW `Person` extendida igual (paridad). `target="_blank"` +`rel="noopener"` en los 3
  (0 sin rel). Cero `href="http://"`. Sin `<table>` en ATS.

## Seguridad GxP aplicada
- Badge `Demo abierta: admin / 1234` → `Demo interactiva disponible` / `Live interactive demo`
  (ES/EN/PREVIEW). PII propio intacto (email+telf en hero + Person). PII terceros con toggle 👁
  intacto en PREVIEW. `print:hidden`/`@media print` intactos.

## Verificación (fase por fase, counts)
| Check | Resultado |
|---|---|
| `6+` en 3 archivos | 0 ✅ |
| `SpringBoot` / `SpringSecurity` | 0 / 0 ✅ |
| ES-en-EN (`Cúcuta\|años\|Tecnólogo\|Intermedio\|Avanzado`) | 0 ✅ |
| `k3d\|AKS\|OWASP` ES / EN / PREVIEW | 19 / 19 / 27 ✅ |
| Bloques `ld+json` ES / EN / PREVIEW | 3 / 3 / 3, JSON válido ✅ |
| Tags balanceados (parser, todos los tags) | 0 sin cerrar, 0 desorden ✅ |
| Certs visibles / details (DOM) | 13 / 8 ✅ |
| `target=_blank` sin `rel=noopener` | 0 ✅ |
| `<table>` en ATS | 0 ✅ |
| PII propio presente | email+telf ✅ |

## Pendientes de AUTORIZACIÓN (no hacer sin Yadin)
1. `sdd-verify` del change (usuario lo revisa primero en worktree).
2. `git add/commit/push` (worktree queda modificado a propósito).
3. Credenciales demo YadinStore en PREVIEW (`demo-creds` + FAQ `1cLaVeDePrUeBa+`): se dejan
   intactas por ser demo solo-lectura publicada por el usuario — confirmar si se retiran.
4. `Curso Profesional Git` en details vs top-13 (cupo); lista final 13 la confirma Yadin.
5. `Industria de Talentos` + `Docker (base)` existen solo en PREVIEW (23 certs) — confirmar
   si se portan a ATS o se quedan como extras del lienzo.
6. Nota: `yadinPortfolio-Reg/avance-2026-09-10.md` figura modificado en worktree por sesión
   anterior — no tocado en esta sesión.

---

## S8 § 2026-09-12 (sub-agente) — Kafka 4/5 + purga 5 chips FRONTEND-IA + port EN

Pedido: 1) Kafka/Event-Driven → ●●●●○ Avanzado. 2) Eliminar de FRONTEND-IA-ECOSISTEMA:
`Custom Post Types`, `Component-Based Architecture`, `Data Sanitization`, `dbDelta`,
`Google Workspace`. 3) Port ES→EN traducido. 4) GxP/OWASP-ASVS. Sin commit/push.

### Hallazgo base (honestidad, no inflar)
- ATS ES Kafka **ya estaba** ●●●●○ Avanzado (`CV_Yadin_Manrique_ATS_2026.html:1144-1145`,
  verificado, no tocado). Faltaban: EN (●●●○○ Intermediate-Advanced), PREVIEW (3/5 dots),
  `Habilidades.jsx` (level 3).
- Tagcloud FRONTEND del PREVIEW (`:2244-2251`) **no contenía** ninguno de los 5 chips
  (solo 11 tags: TailwindCSS…CORS) → sin cambio ahí. Los 5 sí estaban en ats-tags
  FRONTEND de ATS ES (`:1191-1216`) y EN (`:1019-1055`) + `Habilidades.jsx:55`.

### Edits (12, solo skills/chips — prosa/chips de proyectos intactos)
- EN `:969-970`: `●●●○○ Intermediate-Advanced` → `●●●●○ Advanced` ✅
- EN `:1031-1054`: 4 spans fuera (CPT / CBA / Data Sanitization / dbDelta+Google Workspace) ✅
- ES `:1201-1213`: 4 spans fuera (mismos 5) ✅
- PREVIEW `:2149-2157`: dots Kafka 3 filled → **4 filled + 1 empty** ✅
- `src/components/sections/Habilidades.jsx:33`: `level: 3` → `level: 4` ✅
- `Habilidades.jsx:55`: array Frontend-IA 42 → **37 tags** (5 fuera, resto idéntico) ✅
- Remanentes honestos CONSERVADOS a propósito (solo cards/prosa de proyecto):
  ES CPT×2 (`:1043,:1061`), CBA×1 (`:944-945` Insurance CRM), DS×1 (`:946`), dbDelta×2
  (`:1053` prosa + `:1059` chip), GW×1 (`:1094`); EN equivalentes + CBA en prosa `:852`.
  `Habilidades.jsx` y secciones FRONTEND ES/EN/PREVIEW = **0/5** ✅

### Verificación (scripts `C:\Users\yadin\AppData\Local\Temp\opencode\verify_*.py`)
| Check | Resultado |
|---|---|
| Kafka ES / EN / PREVIEW / Habilidades | ●●●●○ Avanzado / ●●●●○ Advanced / 4-5 dots / level 4 ✅ |
| `Intermediate-Advanced` + `Intermedio-Avanzado` (3 HTML) | 0 ✅ |
| Chips 5 en sección FRONTEND (ES/EN/PREVIEW/Habilidades) | 0/0/0/0 ✅ |
| JSON-LD | 3/3 válidos por archivo ✅ |
| Tags balanceados | div/span/section/article/a/em/h4/p 1:1 en ES/EN/PREVIEW ✅ (1 salvedad abajo) |
| `rel=noopener` | ES 41/41, EN 39/39, PREVIEW 40/40 ✅ (ojo: anchors multilínea — grep por línea da falsos +) |
| `<table>` en ATS / `href="http://"` | 0 / 0 ✅ |
| `@media print` (3 HTML) | presente ✅ |
| PII | ATS strict sin PII terceros; PREVIEW toggle 👁 intacto (pii-hidden×6/pii-toggle×4, región no tocada) ✅ |
| Credenciales | ninguna agregada; solo demo solo-lectura preexistente (`demo-creds` + FAQ, decisión pendiente §F6.3) |

### ⚠️ Salvedad preexistente (NO introducida aquí, NO tocada por alcance)
- ATS ES queda con **1 `</b>` stray**: el balance `<b>` va −1 desde **L783-785**
  (card YadinStore e-Shop: falta `<b>` de apertura antes de `Transactional`, cierra
  `Outbox</b>` en L785). HEAD (`git show HEAD:…`) está 107/107 limpio; el +20/+21 `<b>`
  viene de edits de usuario/sesiones en prosa de proyectos. Fix de 1 palabra
  (`Transactional` → `<b>Transactional`) propuesto para `sdd-verify` — está en párrafo
  de proyecto, fuera de mi alcance escribirlo.
- Nota medición: `grep` crudo de chips subcuenta cuando el término se parte en 2 líneas
  (ej. `Component-Based\nArchitecture`); counts de arriba son con matching
  whitespace-normalizado.

### Paridad /src
- `Habilidades.jsx` 1:1 con PREVIEW en niveles (Kafka 4) y tags Frontend (37).
  `Proyectos.jsx` NO tocado (chips de proyecto quedan como fuente honesta).
  `public/llms.txt:27` aún dice `Kafka/Event-Driven (3)` — fuera de alcance,
  flag para próxima sesión si se quiere paridad total.

### Estado git
- Worktree modificado a propósito, **0 staged, sin commit/push** (prohibido).
  `Habilidades.jsx` diff = 4 líneas (2×−/+ mías). Resto del diff en los 3 HTML viene de
  sesiones/usuario previos — mis 10 hunks HTML son solo skill-row/spans/dots.
- PDF `src/components/HV_YadinManrique_2026.pdf` existe ✅ (no editado).
- Next: `sdd-verify` en pausa hasta OK de Yadin/orquestador.

---

## § Auditoría GxP/OWASP global — 2026-09-12 (sub-agente auditor)

**Método:** parseo Python con matching multilínea normalizado (los counts `rel`
son reales, no `grep` por línea que da falsos ±), `git diff`, `git show HEAD`,
`json.loads` (9/9 bloques válidos). Scripts en
`C:\Users\yadin\AppData\Local\Temp\opencode\audit_gxp*.py` (7 scripts).
**El auditor hizo 0 ediciones de código** — solo este apéndice; worktree intacto
para `sdd-verify`.

### Tabla por archivo (counts reales, estado worktree actual)

| Archivo (líneas) | `target=_blank` sin `rel=noopener` | `http://` / `<table>` | JSON-LD | Tags balanceados | `6+` / honestidad | PII / creds | Veredicto |
|---|---|---|---|---|---|---|---|
| ATS ES (1364) | 41/41 con rel ✅ | 0 / 0 ✅ | 3/3 válidos ✅ | 1:1 salvo **`<b>` 127/128** ⚠️ (1 stray L784, preexistente) | `6+`=0, `5+`=5 ✅; Kafka `●●●●○ Avanzado` ✅ | Sin PII terceros ✅; `password`=0; demo solo-lectura preexistente (`demo-creds`×2, `1cLaVe`×1, pendiente §F6.3) | ✅ con salvedad P2 |
| ATS EN (1195) | 39/39 con rel ✅ | 0 / 0 ✅ | 3/3 válidos ✅ | **100% 1:1** ✅ | `6+`=0, ES-residuo=0 ✅; Kafka `●●●●○ Advanced` ✅ | Sin PII terceros ✅; `demo-creds`=0 ✅ | ✅ limpio |
| PREVIEW (2537) | 40/40 con rel ✅ | 0 / n/a ✅ | 3/3 válidos ✅ | **100% 1:1** (`em` 37/37 tras fix 09-12) ✅ | `6+`=0 ✅; Kafka 4 filled+1 empty ✅ | Toggle 👁 intacto (`pii-hidden`×6/`pii-toggle`×4); print oculta `li.contact-li` ✅; demo preexistente (`demo-creds`×7, pendiente §F6.3) | ✅ limpio |
| `Habilidades.jsx` (123) | n/a (sin anchors) | 0 / 0 ✅ | n/a | sintaxis intacta (diff = 2 hunks, 4 líneas) ✅ | Kafka `level: 4` ✅; Frontend **38 tags, 0/5** ✅ | n/a | ✅, con corrección T1 |
| `Proyectos.jsx` (tocado 09-12) | 5/5 con rel ✅ | 0 ✅ | n/a | n/a | `K3d`→`k3d`, `SpringBoot`→`Spring Boot` ✅; `6+`=0 ✅ | `demo` solo-lectura intacto (no inventado) | ✅ (cambios ya documentados en avance-09-12) |
| `Inicio.jsx` / `Certificaciones.jsx` | 3/3 y 1/1 con rel ✅ | 0 ✅ | n/a | n/a | 2 certs nuevas con ID+URL verificación ✅ | n/a | ✅ |
| `public/llms.txt` (54, fuera de alcance) | n/a | n/a | n/a | n/a | ⚠️ **L3 `6+ años`, L11 `6 años`, L27 `Kafka(3)`** — contradice 5+/nivel 4 | n/a | ⚠️ pendiente P1 |

Transversales: `@media print` presente en los 3 HTML ✅; `viewport`+responsive ✅;
`aria-*` 7/7/6 ✅; `SpringBoot`/`SpringSecurity` = 0 en los 3 ✅;
`Intermediate-Advanced`/`Intermedio-Avanzado` = 0 ✅; certs DOM **13 visibles +
8 details = 21** en ES/EN ✅ (H2 EN `13 featured + 8 more` ✅; H2 ES ver P3);
purga 5 chips: secciones FRONTEND ES/EN/PREVIEW = **0/5** ✅, remanentes solo en
cards de proyecto (chips+prosa, mapa consistente con §S8) ✅;
`Co-Authored-By` en últimos 8 commits = 0 ✅; 0 staged, sin commit/push ✅;
PDF existe (modificado por usuario, no tocado) ✅.

### Correcciones a trazabilidad previa (GxP: docs dicen la verdad)

- **T1 — §S8 "Frontend 42→37" es incorrecto.** Real verificado (`git show HEAD`
  vs worktree, mismo regex): **43 → 38 tags** (5 eliminados, 0/5 remanentes).
- **T2 — chips viejos desactualizados.** `span.chip` actuales:
  ES **195** / EN **184** / PREVIEW **206** (el avance decía 220/252 de cuando los
  archivos eran más chicos). Crecimiento por edits de usuario posteriores:
  ES 1016→**1364**, EN 987→**1195**, PREVIEW 2474→**2537** líneas.
- **T3 — token literal `print:hidden` no existe** en ningún archivo (0
  ocurrencias). El mecanismo real (verificado): bloque `@media print` +
  `li.contact-li{display:none!important}` en print + `display:none` inline en
  `.pii-hidden`. Funciona; el avance lo nombraba de forma imprecisa.

### Pendientes (ninguno crítico; todos exceden alcance o requieren a Yadin)

- **P1 (nuevo): `public/llms.txt`** — `6+`/`6 años`/`Kafka(3)` contradicen la
  corrección honesta 5+/nivel 4. Archivo fuera de alcance → orquestador/Yadin
  deciden sesión de port.
- **P2 (preexistente): stray `</b>` ATS ES L784**
  (`<b></b>Transactional…Outbox</b>`, balance 127/128). Prosa de proyecto,
  no crítico (browsers/ATS lo toleran) → fix de 1 palabra en `sdd-verify`.
- **P3 (nuevo): H2 certs ES** dice `13 destacadas · **146 verificables**`,
  inconsistente con 21 items DOM y con EN (`13 featured + 8 more`). Probable typo
  de edición de usuario post-F5 → aclarar con Yadin, no adivinar.
- **P4 (preexistente §F6.3): creds demo solo-lectura en PREVIEW** (`demo-creds`×7,
  `1cLaVeDePrUeBa`×3) — decisión de retiro pendiente de Yadin. No introducido
  por ninguna sesión de sub-agente.
- **P5: `avance-2026-09-10.md` modificado en worktree por sesión previa** —
  no tocado por esta auditoría.

### Veredicto

**APTO para `sdd-verify` con las 5 salvedades (P1–P5) documentadas.**
Cero gaps críticos OWASP/ASVS/GxP en el alcance auditado. Fixes del auditor: 0
(los 2 candidatos — print `.pii-hidden` y stray `</b>` — se descartaron con
evidencia: el primero ya está cubierto, el segundo es prosa fuera de alcance).
Reglas repo: sin `Co-Authored-By`, sin build, sin commit/push — respetadas.

---

## § Apply cv-ats-2026 — 2026-09-12 (sub-agente implementador) — llms.txt honesto + H2 21/LinkedIn

Alcance autorizado por Yadin vía orquestador: 1) `public/llms.txt` (+ espejo
`dist/llms.txt`) — `6+`→`5+`, `Kafka(3)`→`Kafka(4)`. 2) H2 certs ES/EN —
`21 principales en CV + resto en LinkedIn` con URL exacta
`https://www.linkedin.com/in/yadin-paulo-manrique-marquez-18753910/`.
Sin `/src`, sin build, sin commit/push. Skill k8s-basics cargada.

### Edits (5)
- `public/llms.txt:3`: `6+ años` → `5+ años` ✅
- `public/llms.txt:11`: `con 6 años de evolución` → `con 5+ años de evolución` ✅
- `public/llms.txt:27`: `Kafka/Event-Driven (3)` → `Kafka/Event-Driven (4)` ✅
- `dist/llms.txt`: espejo sincronizado con los mismos 3 cambios (era
  byte-idéntico a `public/` antes del edit; sigue siéndolo) ✅
- `CV_Yadin_Manrique_ATS_2026.html:1221-1223`: H2
  `13 destacadas · 146 verificables · ver todas…` →
  `Certificaciones — 21 principales en CV · resto verificables en LinkedIn →`
  (link exacto autorizado, `target=_blank rel=noopener`, estilo inline intacto) ✅
- `CV_Yadin_Manrique_ATS_2026_EN.html:1058`: H2
  `13 featured + 8 more` →
  `Certifications — 21 featured in CV · rest verifiable on LinkedIn →`
  (mismo link exacto, traducido) ✅
- PREVIEW NO tocado (`<h2><span>//</span> Certificaciones</h2>` sin conteo —
  nada deshonesto que corregir; tocarlo habría inventado un número) ✅
- `<details class="ats-more"><summary>+8…</summary>` intacto en ES/EN
  (13+8=21, consistente con el nuevo H2) ✅

### Verificación
| Check | Resultado |
|---|---|
| `6+ años`/`6 años`/`6+ yrs` en `public/llms.txt`, `dist/llms.txt`, ATS ES/EN, PREVIEW | 0 ✅ |
| `5+ años` en ambos `llms.txt` (L3+L11) | presente ✅ |
| `Kafka/Event-Driven (4)` en ambos `llms.txt` (L27) | presente ✅ |
| `146` en llms×2 + ATS ES/EN | 0 ✅ |
| H2 ES `21 principales` / H2 EN `21 featured` | 1/1 ✅ |
| LinkedIn URL exacta en ES+EN (JSON-LD+contacto+H2) | 6 ocurrencias ✅ |
| JSON-LD | 3/3 bloques válidos por archivo (ES+EN) ✅ |
| Tags balanceados (parser) | EN limpio; ES 1 stray `</b>` L785 preexistente (P2, prosa fuera de alcance, NO tocado) ✅ |
| `target=_blank` nuevo en H2 EN | con `rel=noopener` ✅ |
| Single-column ATS / print CSS / `.ats-more` script | intactos (diff = solo línea H2) ✅ |

### Estado git
Worktree modificado a propósito, 0 staged, sin commit/push (prohibido).
Archivos tocados: `public/llms.txt`, `dist/llms.txt`,
`CV_Yadin_Manrique_ATS_2026.html`, `CV_Yadin_Manrique_ATS_2026_EN.html`,
este avance (append-only).
Next: `sdd-verify` del change cv-ats-2026.

## § Verify sdd-verify cv-ats-2026 — 2026-09-12 (sub-agente verificador)

Base: proposal/design/tasks engram (#798/#799/#800, full vía
`mem_get_observation`); sin spec ni apply-progress (documentado, no bloquea).
Skill k8s-basics cargada. Lectura + counts pwsh/regex, 0 build (prohibido),
0 commit/push. Reporte full en engram
`topic_key: sdd/cv-ats-2026/verify-report`.

### Resultado
**PASS WITH WARNINGS** — 11/13 tasks (4.2 curl sin red + 4.3 este verify).

| Check | Resultado |
|---|---|
| F1 headline k3d/AKS/OWASP | ES 12/11/15 · EN 11/10/15 · PREVIEW 12/12/14 ✅ |
| F2 `6+`=0 / `5+` / `146`=0 / Back renombrado | 0 · 5/6/4 · 0 · 0 ✅ |
| F3 ES-en-EN=0 · Cucuta/Cúcuta | 0 · 6/0 ✅ |
| F4 SpringBoot/SpringSecurity=0 · 5 chips FRONTEND | 0/0 · 0/0 ✅ |
| F5 certs 13+8 ES/EN · H2 21 + LinkedIn exacta | ✅ |
| F6 JSON-LD 3/3 válidos ×3 · Person SENA/ITIL/COMFAORIENTE · ItemList+FAQ | ✅ |
| Links `target=_blank` sin `rel=noopener` | 0/0/0 (41/41, 40/40, 40/40) ✅ |
| `<table>` / `http://` / PII en meta / creds literales | 0/0/0/0 ✅ |
| llms `5+`, Kafka (4), dist espejo idéntico | ✅ |

### Issues
- **P2 FIXED** (autorizado 1 palabra): stray `</b>` ES L784
  (`<b></b>Transactional` → `<b>Transactional`). Re-verificado b=127/127.
- **P1 WARNING**: `llms.txt:30` dice "12 top + 9" pero DOM = 13 + 8
  (total 21 = 21, split difiere) → corregir a "13 top + 8" antes de push.
- **WARNING scope-creep**: fuera de "SOLO 3×CV_*" hay `src/*.jsx` (4),
  PDF binario, avance-09-10 +52 (P5), avance-09-12 untracked → stage
  selectivo in-scope al pushear.
- **WARNING 4.2**: sin muestra curl de certs (sin red).
- Sin commit/push (prohibido hasta autorización Yadin).
Next: corregir P1 → push selectivo → `sdd-archive`.
