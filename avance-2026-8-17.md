# Avance 2026-08-17 — Credenciales demo en cards YadinStore (mode porcelain)

**Fecha:** 2026-08-17
**Objetivo:** Agregar credenciales demo (`usuario.demo@yadinstore.com | 1cLaVeDePrUeBa+`) a las 2 primeras cards de YadinStore (e-Shop SaaS y Admin Dashboard) del portafolio, con verificación de seguridad previa (claim del usuario puesto a prueba) y trabajando SOLO sobre `CV_Fusion_FE_2026_PREVIEW.html` hasta aprobación → luego replicado en `src/components/sections/Proyectos.jsx`.

---

## 1. Verificación de seguridad (análisis read-only de `C:\GITHUB\YadinStore`)

**El claim del usuario "es de solo lectura por lo que es altamente seguro" fue PUESTO A PRUEBA con evidencia del código** — hallazgos:

| # | Hallazgo | Evidencia | Estado |
|---|----------|-----------|--------|
| 1 | **"Solo lectura" era FALSO**: `POST /api/v1/orders`, checkout (`CartController:79,92`) y pagos (`PaymentController:45,56,67`) solo exigían `authenticated()` → un VIEWER podía crear órdenes/pagos | `SecurityConfig.java:273` (default) — sin `@PreAuthorize` en esos métodos | 🔒 **CORREGIDO por el usuario** (VIEWER sin compras, conserva `/cart`) |
| 2 | **Escalada de privilegios**: `POST /api/v1/auth/register` (permitAll) aceptaba `role` sin validar → cualquier anónimo podía auto-registrarse `ADMIN` | `AuthService.java:136` `user.setRole(request.role() != null ? request.role().name() : "CUSTOMER")` | 🔒 **CORREGIDO** (role != CUSTOMER = 400 + FE sin select de rol) |
| 3 | **Riesgo inminente en monorepo**: `.env` con credenciales reales + passwords en docs de spec, con push a público pendiente | `avance17-8-2026.md:78`, `comando para loguearse.txt:8` | 🔒 **CORREGIDO** (`.gitignore` MFE-DA + docs limpiados; repo privado, sin rotación) |
| 4 | Rate limiting en login: SÍ (30/min por IP, custom in-memory) | `AuthRateLimitFilter.java:29` | ✅ OK |
| 5 | Datos: 100% fake (tractores ficticios, `cliente1-4@yadinstore.com`) | `SeedDemoDashboardData.java:48-73` | ✅ OK |
| 6 | IDOR mitigado en GETs; excepción `GET /orders` (lista todas del tenant) | `OrderService.java:110-118` | 🔒 **CORREGIDO** (filtro por userId para CUSTOMER/BUYER/VIEWER, ASVS 4.2.1) |

**Fixes aplicados por el usuario en YadinStore (5 repos, deploy completo):** register validado, VIEWER sin compras, IDOR/BOLA filtrado, idempotencia (header `Idempotency-Key` + índice único, ASVS 10.1-10.3), `.gitignore` MFE-DA, docs con passwords → referencia a `.env`. **43 tests, 0 fallos, build EXIT 0.**

**Pendiente heredado (decisión del usuario):** `DEMO_USER_PASSWORD` en Render sin actualizar (pendiente #2 del avance de YadinStore).

---

## 2. Preview (mode porcelain — primer paso, aprobado en Chrome)

- **Bloque `.demo-creds`** insertado entre `.chips` y `.link` en las cards 1 (e-Shop) y 2 (Admin) de `CV_Fusion_FE_2026_PREVIEW.html`.
- **CSS nuevo** (consistente con variables del preview: `--muted`, `--green`, `--green-soft`): flex wrap, borde dashed `rgba(148,163,184,.35)`, fondo `rgba(148,163,184,.08)`, `code` mono con `var(--green)`.
- **Verificación:** `pcard: 13`, `badge: 13`, `articles 13/13`, `demo-creds: 2`, email/password `2` apariciones c/u, `links: 13`, UTF-8 íntegro.
- **Aprobado por el usuario** viendo el archivo en Chrome.

## 3. Replicación en JSX (opción A aprobada — campo `demo` + render condicional)

**`src/components/sections/Proyectos.jsx` — 3 cambios, patrón prolijo:**
1. Card 1 (e-Shop): `demo: { email: "usuario.demo@yadinstore.com", password: "1cLaVeDePrUeBa+" }`
2. Card 2 (Admin): ídem
3. Render condicional tras los chips: `{project.demo && ( ... )}` — bloque con label "Demo · solo lectura:", 2 `<code>` mono verde sobre `bg-white/5` borde dashed.

**Verificación estática:** `title/badge/badgeClass/chips/href: 13/13/13/13/13` · `demo (campo): 2` · `demo (render): 3` · `},`: 15 (13 objetos + 2 inline demo).
**Diff vs HEAD (`9cd2dcf`): 15 inserciones, 0 eliminaciones, 0 modificaciones** → las otras 11 cards NO se tocaron ni un byte.

**Decisión de diseño:** patrón A elegido sobre pegar HTML en la descripción → cualquier card futura agrega credenciales con UNA línea de datos; el componente se encarga. El badge de la card Pokédex ya usaba el patrón "Demo abierta: admin / 1234" en el badge; esta vez se optó por bloque visual dedicado (más claro, copy-paste amigable).

---

## 4. Pendiente (esperando orden del usuario)

- 🔲 `npm run build` + `git add` + commit conventional corto + `git push` + `npm run deploy` (ciclo completo; el preview HTML queda trackeado como decisión previa del 2026-08-13).
